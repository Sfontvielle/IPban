/**
 * Аварийный режим запуска — для поиска причины краша на устройстве.
 *
 * Если приложение падает на телефоне без экрана ошибки, значит упал нативный код,
 * и его не видно ни в терминале, ни в JavaScript. Тогда меняем значение ниже,
 * перезапускаем `npx expo start -c` и смотрим, на каком уровне падение исчезает.
 *
 *   0 — обычная работа
 *   1 — без базы данных (интерфейс есть, данных нет)
 *   2 — только текст: ни базы, ни темы, ни навигации по вкладкам
 *
 * Уровень, на котором приложение перестаёт падать, и указывает на виновника.
 */
export const DIAGNOSTIC_MODE: 0 | 1 | 2 = 0;
